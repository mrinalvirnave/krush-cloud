apiVersion: v1
kind: Service
metadata:
  name: {{ include "repo-register.fullname" . }}
  labels:
    {{- include "repo-register.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "repo-register.selectorLabels" . | nindent 4 }}
