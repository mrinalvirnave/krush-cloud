apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "repo-register.fullname" . }}-test-connection"
  labels:
    {{- include "repo-register.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "repo-register.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
